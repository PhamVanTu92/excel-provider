# Enforce LF line endings for shell scripts (Windows → Linux compatibility)
*.sh    text eol=lf

# Keep CRLF for Windows-native files
*.bat   text eol=crlf
*.cmd   text eol=crlf

# Auto-detect everything else
*       text=auto
